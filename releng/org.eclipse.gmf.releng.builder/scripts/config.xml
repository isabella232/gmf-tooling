<?xml version="1.0"?>
<cruisecontrol>

	<property file="cc.properties" />
	<property file="label.properties" />

	<plugin name="htmlemail" mailhost="${mailhost}" skipusers="true"
		returnaddress="${buildmaster.email}" returnname="Buildmaster"
		css="${scripts}/css/cruisecontrol.css"
		xsldir="${scripts}/xsl"
	/>

	<project name="checkout" buildafterfailed="true">
		<listeners>
			<currentbuildstatuslistener
				file="${logs}/checkout/buildstatus.txt" /> 
			<currentbuildstatusftplistener
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}"
				destdir="/"
				file="${remotelogs}/checkout/buildstatus.txt" />
		</listeners>

		<!-- Bootstrappers are run every time the build runs, 
			*before* the modification checks -->
		<bootstrappers></bootstrappers>

		<!-- Defines where cruise looks for changes, to decide
			whether to run the build -->
		<modificationset quietperiod="10">
			<cvs
				cvsroot=":pserver:anonymous@dev.eclipse.org:/cvsroot/technology"
				module="org.eclipse.gmf/plugins" />
				<cvs
				cvsroot=":pserver:anonymous@dev.eclipse.org:/cvsroot/technology"
				module="org.eclipse.gmf/features" />
			<cvs
				cvsroot=":pserver:anonymous@dev.eclipse.org:/cvsroot/technology"
				module="org.eclipse.gmf/tests" />
			<cvs
				cvsroot=":pserver:anonymous@dev.eclipse.org:/cvsroot/technology"
				module="org.eclipse.gmf/examples" />
		</modificationset>

		<!-- Configures the actual build loop, how often and which
			build file/target -->
		<schedule interval="1200">
			<!-- Perform checkout of all plug-ins and features, as builder uses export. -->
			<ant antscript="ant.${ext}" buildfile="checkout.xml"
				target="all" uselogger="true" usedebug="false" usequiet="true">
			</ant>
		</schedule>

		<!-- directory to write build logs to -->
		<log dir="${logs}/checkout" />
		<publishers>
			<ftppublisher destDir="${remotelogs}/checkout" srcDir="${logs}/checkout"
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}" />
		</publishers>
	</project>
	
	<project name="continuous" buildafterfailed="true">
		<listeners>
			<currentbuildstatuslistener
				file="${logs}/continuous/buildstatus.txt" />
			<currentbuildstatusftplistener
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}"
				destdir="/"
				file="${remotelogs}/continuous/buildstatus.txt" />
		</listeners>

		<!-- Bootstrappers are run every time the build runs, 
			*before* the modification checks -->
		<bootstrappers></bootstrappers>

		<!-- Defines where cruise looks for changes, to decide
			whether to run the build -->
		<modificationset>
<!--		    <alwaysbuild/>  -->
		    <buildstatus logdir="${logs}/checkout" />
		</modificationset>

		<!-- Configures the actual build loop, how often and which
			build file/target -->
		<schedule interval="300">
			<ant antscript="build.${ext}" buildfile="buildAll.xml"
				target="main" uselogger="true" usedebug="false" usequiet="true">
				<property name="fetchTag" value="HEAD" />
				<property name="buildType" value="C" />
				<property name="forceContextQualifier" value="v${date}-${time}" />
				<property name="skipJavadoc" value="true" />			
			</ant>
		</schedule>

		<!-- directory to write build logs to -->
		<log dir="${logs}/continuous">
			<merge dir="${artifacts}/cc" />
		</log>

		<!-- Publishers are run *after* a build completes -->
		<publishers>
			<htmlemail buildresultsurl="${reporting.url}/buildresults/continuous"
			logdir="${logs}/continuous">
				<always address="${always.email}" />
			</htmlemail>
			<artifactspublisher dir="${artifacts}/cc"
				dest="${cc.publish}/continuous/" />
			<ftppublisher destDir="${remotelogs}/continuous" srcDir="${logs}/continuous"
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}" />
		</publishers>
	</project>
	
	<project name="nightly" buildafterfailed="true">
		<listeners>
			<currentbuildstatuslistener
				file="${logs}/nightly/buildstatus.txt" />
			<currentbuildstatusftplistener
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}"
				destdir="/"
				file="${remotelogs}/nightly/buildstatus.txt" />
		</listeners>

		<!-- Bootstrappers are run every time the build runs, 
			*before* the modification checks -->
		<bootstrappers></bootstrappers>

		<!-- Defines where cruise looks for changes, to decide
			whether to run the build -->
		<modificationset>
			<alwaysbuild /> 
		</modificationset>

		<!-- Configures the actual build loop, how often and which
			build file/target -->
		<schedule>
			<ant time="0600" antscript="build.${ext}" buildfile="buildAll.xml"
				target="nightly" uselogger="true" usedebug="false" usequiet="true">				
				<property name="fetchTag" value="HEAD" />
				<property name="buildType" value="N" />
				<property name="forceContextQualifier" value="v${date}-${time}" />
			</ant>
		</schedule>

		<!-- directory to write build logs to -->
		<log dir="${logs}/nightly">
			<merge dir="${artifacts}/cc" />
		</log>

		<!-- Publishers are run *after* a build completes -->
		<publishers>
			<htmlemail buildresultsurl="${reporting.url}/buildresults/nightly"
			logdir="${logs}/nightly">
				<always address="${always.email}" />
			</htmlemail>
			<artifactspublisher dir="${artifacts}/cc"
				dest="${cc.publish}/nightly/" />
			<ftppublisher destDir="${remotelogs}/nightly" srcDir="${logs}/nightly"
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}" />
		</publishers>
	</project>
	<project name="integration" buildafterfailed="true">
		<listeners>
			<currentbuildstatuslistener
				file="${logs}/integration/buildstatus.txt" />
			<currentbuildstatusftplistener
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}"
				destdir="/"
				file="${remotelogs}/integration/buildstatus.txt" />
		</listeners>

		<bootstrappers></bootstrappers>

		<modificationset>
			<alwaysbuild /> 
		</modificationset>

		<schedule>
			<ant time="0300" antscript="build.${ext}" buildfile="buildAll.xml"
				target="integration" uselogger="true" usedebug="false" usequiet="true">				
				<property name="fetchTag" value="HEAD" />
				<property name="buildType" value="I" />
				<property name="forceContextQualifier" value="v${date}-${time}" />
				<property name="skipUpload" value="true" />
			</ant>
		</schedule>

		<log dir="${logs}/integration">
			<merge dir="${artifacts}/cc" />
		</log>

		<publishers>
			<htmlemail buildresultsurl="${reporting.url}/buildresults/integration"
			logdir="${logs}/integration">
				<always address="${always.email}" />
			</htmlemail>
			<artifactspublisher dir="${artifacts}/cc"
				dest="${cc.publish}/integration/" />
			<ftppublisher destDir="${remotelogs}/integration" srcDir="${logs}/integration"
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}" />
		</publishers>
	</project>
	
	<project name="checkout.maintenance" buildafterfailed="true">
		<listeners>
			<currentbuildstatuslistener
				file="${logs}/checkout.maintenance/buildstatus.txt" /> 
			<currentbuildstatusftplistener
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}"
				destdir="/"
				file="${remotelogs}/checkout.maintenance/buildstatus.txt" />
		</listeners>

		<!-- Bootstrappers are run every time the build runs, 
			*before* the modification checks -->
		<bootstrappers></bootstrappers>

		<!-- Defines where cruise looks for changes, to decide
			whether to run the build -->
		<modificationset quietperiod="10">
			<cvs
				cvsroot=":pserver:anonymous@dev.eclipse.org:/cvsroot/technology"
				module="org.eclipse.gmf/plugins" />
				<cvs
				cvsroot=":pserver:anonymous@dev.eclipse.org:/cvsroot/technology"
				module="org.eclipse.gmf/features" />
			<cvs
				cvsroot=":pserver:anonymous@dev.eclipse.org:/cvsroot/technology"
				module="org.eclipse.gmf/tests" />
			<cvs
				cvsroot=":pserver:anonymous@dev.eclipse.org:/cvsroot/technology"
				module="org.eclipse.gmf/examples" />
		</modificationset>

		<!-- Configures the actual build loop, how often and which
			build file/target -->
		<schedule interval="1200">
			<!-- Perform checkout of all plug-ins and features, as builder uses export. -->
			<ant antscript="ant.${ext}" buildfile="checkout.xml"
				target="all" uselogger="true" usedebug="false" usequiet="true">
				<property name="tag" value="R1_0_maintenance"/>
			</ant>
		</schedule>

		<!-- directory to write build logs to -->
		<log dir="${logs}/checkout.maintenance" />
		<publishers>
			<ftppublisher destDir="${remotelogs}/checkout.maintenance" srcDir="${logs}/checkout.maintenance"
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}" />
		</publishers>
	</project>

	<project name="maintenance" buildafterfailed="true">
		<listeners>
			<currentbuildstatuslistener
				file="${logs}/maintenance/buildstatus.txt" />
			<currentbuildstatusftplistener
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}"
				destdir="/"
				file="${remotelogs}/maintenance/buildstatus.txt" />
		</listeners>

		<!-- Bootstrappers are run every time the build runs, 
			*before* the modification checks -->
		<bootstrappers></bootstrappers>

		<!-- Defines where cruise looks for changes, to decide
			whether to run the build -->
		<modificationset>
<!--		    <alwaysbuild/>  -->
		    <buildstatus logdir="${logs}/checkout.maintenance" />
		</modificationset>

		<!-- Configures the actual build loop, how often and which
			build file/target -->
		<schedule interval="300">
			<ant antscript="build.${ext}" buildfile="buildAll.xml"
				target="maintenance" uselogger="true" usedebug="false" usequiet="true">
				<property name="fetchTag" value="R1_0_maintenance" />
				<property name="buildType" value="M" />
				<property name="forceContextQualifier" value="v${date}-${time}" />			
				<property name="skipUpload" value="true" />
				<property name="skipJavadoc" value="true" />			
			</ant>
		</schedule>

		<!-- directory to write build logs to -->
		<log dir="${logs}/maintenance">
			<merge dir="${artifacts}/cc" />
		</log>

		<!-- Publishers are run *after* a build completes -->
		<publishers>
			<htmlemail buildresultsurl="${reporting.url}/buildresults/maintenance"
			logdir="${logs}/maintenance">
				<always address="${always.email}" />
			</htmlemail>
			<artifactspublisher dir="${artifacts}/cc"
				dest="${cc.publish}/maintenance/" />
			<ftppublisher destDir="${remotelogs}/maintenance" srcDir="${logs}/maintenance"
				targetHost="${ccweb.ftp.host}"
				targetUser="${ccweb.ftp.user}"
				targetPasswd="${ccweb.ftp.passwd}" />
		</publishers>
	</project>
	
	
</cruisecontrol>
