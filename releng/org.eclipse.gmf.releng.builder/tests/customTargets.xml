<project name="Build specific targets and properties" default="noDefault">
	
	<!-- ===================================================================== -->
	<!-- Run a given ${target} on all elements being built -->
	<!-- Add on <ant> task for each top level element being built. -->
	<!-- ===================================================================== -->
	<target name="allElements">
		
		<ant antfile="${genericTargets}" target="${target}">
			<property name="type" value="feature" />
			<property name="id" value="${product}"/>
		</ant>
	</target>

	<!-- ===================================================================== -->
	<!-- Targets to assemble the built elements for particular configurations  -->
	<!-- These generally call the generated assemble scripts (named in -->
	<!-- ${assembleScriptName}) but may also add pre and post processing -->
	<!-- Add one target for each root element and each configuration -->
	<!-- ===================================================================== -->

	<target name="assemble.org.eclipse.gmf.tests">
		<ant antfile="${assembleScriptName}" dir="${buildDirectory}"/>
	</target>	

	<!-- ===================================================================== -->
	<!-- Check out map files from correct repository -->
	<!-- Replace values for mapsCheckoutTag as desired. -->
	<!-- ===================================================================== -->
	<target name="getMapFiles" depends="checkLocalMaps" unless="skipMaps">
		<property name="mapsCheckoutTag" value="HEAD" />
		<cvs command="checkout -d org.eclipse.gmf.releng" cvsRoot="${mapsRepo}" package="${mapsRoot}" dest="${buildDirectory}/maps" tag="${mapsCheckoutTag}" />
	</target>

	<target name="checkLocalMaps">
		<available property="skipMaps" file="${buildDirectory}/maps" />
	</target>

	<target name="tagMapFiles" if="tagMaps">
		<cvs dest="${buildDirectory}/maps/${mapsRoot}" command="tag ${mapsTagTag}" />
	</target>

	<!-- ===================================================================== -->

	<target name="clean" unless="noclean">
		<antcall target="allElements">
			<param name="target" value="cleanElement" />
		</antcall>
	</target>

	<target name="gatherLogs">
		<mkdir dir="${buildDirectory}/${buildLabel}/compilelogs" />
		<antcall target="allElements">
			<param name="target" value="gatherLogs" />
		</antcall>
		<unzip dest="${buildDirectory}/${buildLabel}/compilelogs" overwrite="true">
			<fileset dir="${buildDirectory}/features">
				<include name="**/*.log.zip" />
			</fileset>
		</unzip>
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before setup -->
	<!-- ===================================================================== -->
	<target name="preSetup">
		<antcall target="cleanBase"/>
	</target>
	
	<target name="cleanBase" if="cleanBase">
		<delete dir="${baseLocation}"/>
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after setup but before starting the build proper -->
	<!-- ===================================================================== -->
	<target name="postSetup">
		<!-- Load build.cfg properties -->
		<property file="${buildDirectory}/maps/org.eclipse.gmf.releng/maps/build.cfg"/>
		<antcall target="getBaseComponents" />
		<available file="${baseLocation}/configuration/config.ini" property="baseExists" />
		<antcall target="installBase" />
		<available file="${baseLocationRoot}/eclipse-test-framework-${eclipseVersion}.zip" property="testingFrameworkExists" />
		<antcall target="getTestingFramework" />
		<antcall target="installTestingFramework" />
<!-- TODO: this is a poor way to check for an installation... the update manager
     allows for querying an installation, but just returns a list that would require parsing. -->
		<available file="${baseLocation}/plugins/org.eclipse.gef_${gefVersion}.jar" property="gefExists" />
		<antcall target="installGEF" />
		<available file="${baseLocation}/plugins/org.eclipse.emf_${emfVersion}.jar" property="emfExists" />
		<antcall target="installEMF" />		
		<antcall target="installGMF" />	
	</target>
	
	<!-- ===================================================================== -->
	<!-- Retrieve and install Eclipse base and dependencies. -->
	<!-- ===================================================================== -->
	<target name="getBaseComponents" depends="checkLocalBase" unless="skipBase">
<!-- TODO: find a more reliable method! -->
		<get src="${eclipseBaseURL}" dest="${baseLocationRoot}/eclipse-SDK-${eclipseVersion}-${baseos}.zip" verbose="true" usetimestamp="true"/>
	</target>
	
	<target name="getTestingFramework" unless="testingFrameworkExists">
	<!-- TODO: find a more reliable method! -->
		<get src="${testingFrameworkURL}" dest="${baseLocationRoot}/eclipse-test-framework-${eclipseVersion}.zip" verbose="true" usetimestamp="true"/>
	</target>

	<target name="checkLocalBase">
		<available file="${baseLocationRoot}/eclipse-SDK-${eclipseVersion}-${baseos}.zip" property="skipBase" />
	</target>
	
	<target name="installBase" unless="baseExists">
		<unzip dest="${baseLocationRoot}/" overwrite="true" src="${baseLocationRoot}/eclipse-SDK-${eclipseVersion}-${baseos}.zip" />
	</target>
	
	<target name="installTestingFramework">
		<unzip dest="${baseLocationRoot}/" overwrite="true" src="${baseLocationRoot}/eclipse-test-framework-${eclipseVersion}.zip" />
	</target>
	
	<target name="installEMF" unless="emfExists">		
		<ant antfile="${scripts}/update.xml" target="installFeature">
			<property name="featureId" value="${emfFeatureId}" />
			<property name="version" value="${emfVersion}" />
			<property name="updateSite" value="${emfUpdateSite}" />
		</ant>
	</target>

	<target name="installGEF" unless="gefExists">
		<ant antfile="${scripts}/update.xml" target="installFeature">
			<property name="featureId" value="${gefFeatureId}" />
			<property name="version" value="${gefVersion}" />
			<property name="updateSite" value="${gefUpdateSite}" />
		</ant>
	</target>
	
	<target name="installGMF" unless="gmfExists">		
		<antcall target="installGMFfromMirror"/>
		<antcall target="installGMFfromZip"/>
	</target>
	
	<target name="installGMFfromMirror" if="usingMirror">
		<echo message="Installing GMF for testing from local mirror..."/>
		<ant antfile="${scripts}/update.xml" target="install">
			<property name="featureId" value="org.eclipse.gmf.sdk" />
			<property name="version" value="${gmfVersion}" />
		</ant>
	</target>
	
	<target name="installGMFfromZip" unless="usingMirror">
		<echo message="Installing GMF for testing from zip file..."/>
		<unzip dest="${baseLocationRoot}/" overwrite="true" src="${artifacts}/gmf-sdk/org.eclipse.gmf.sdk-${buildId}.zip" />
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before fetching the build elements -->
	<!-- ===================================================================== -->
	<target name="preFetch">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after fetching the build elements -->
	<!-- ===================================================================== -->
	<target name="postFetch">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before generating the build scripts. -->
	<!-- ===================================================================== -->
	<target name="preGenerate">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after generating the build scripts. -->
	<!-- ===================================================================== -->
	<target name="postGenerate">
		<antcall target="clean" />
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before running the build.xmls for the elements being built. -->
	<!-- ===================================================================== -->
	<target name="preProcess">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after running the build.xmls for the elements being built. -->
	<!-- ===================================================================== -->
	<target name="postProcess">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before running assemble. -->
	<!-- ===================================================================== -->
	<target name="preAssemble">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after  running assemble. -->
	<!-- ===================================================================== -->
	<target name="postAssemble">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do before running package. -->
	<!-- ===================================================================== -->
	<target name="prePackage">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after  running package. -->
	<!-- ===================================================================== -->
	<target name="postPackage">
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do after the build is done. -->
	<!-- ===================================================================== -->
	<target name="postBuild">
		<property file="${buildDirectory}/maps/org.eclipse.gmf.releng/maps/build.cfg"/>		
		<antcall target="gatherLogs" />
		<!-- A temporary directory leftover? -->
		<delete dir="${buildDirectory}/org.eclipse.gmf" failonerror="false"/>
		<!-- Consolidate artifacts -->
		<move file="${buildDirectory}/${buildLabel}" tofile="${artifacts}/gmf-tests"/>		
		<antcall target="generateUpdateSite"/>
		<!-- Execute tests. -->
		<antcall target="test"/>
		<!-- Uninstall GMF from base. -->
		<echo message="Uninstalling GMF from base..."/>
		<ant antfile="${scripts}/update.xml" target="uninstall">
			<property name="featureId" value="org.eclipse.gmf.sdk" />
			<property name="version" value="${gmfVersion}" />
		</ant>
	</target>
		
	<!-- ===================================================================== -->
	<!-- Update the update site. -->
	<!-- ===================================================================== -->	
	<target name="generateUpdateSite">
	  	<!-- Create the directory structure -->
	  	<mkdir dir="${buildUpdateSitePath}/features"/>
	  	<mkdir dir="${buildUpdateSitePath}/plugins"/>
	  	<!-- Build the jar files -->
	  	<antcall target="allElements">
			<param name="genericTargets" value="${builder}/customTargets.xml"/>
			<param name="target" value="updateSiteExport"/>
	  	</antcall>
	</target>
	
	<target name="updateSiteExport">
	  <ant antfile="build.xml" dir="${buildDirectory}/features/${product}/" target="build.update.jar">
	    <property name="feature.destination" value="${buildUpdateSitePath}/features"/>
	    <property name="plugin.destination" value="${buildUpdateSitePath}/plugins"/>
	  </ant>		
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do to test the build results -->
	<!-- TODO: improve packaging for easier standalone testing? -->
	<!-- ===================================================================== -->
	<target name="test" unless="skipTests">
		<antcall target="installTests"/>
	  	<echo message="Executing automated tests for GMF..."/>
		<java jar="${baseLocation}/startup.jar" fork="true" failonerror="true">
			<arg value="-application"/>
			<arg value="org.eclipse.ant.core.antRunner"/>			
			<arg value="-file"/>
			<arg value="${baseLocation}/plugins/${product}_${gmfVersion}/test.xml"/>
			<arg value="-logger"/>
			<arg value="org.apache.tools.ant.DefaultLogger"/>
			<arg value="-Dos=${baseos}"/>
			<arg value="-Dws=${basews}"/>
			<arg value="-Darch=${basearch}"/>
			<arg value="-DtestPlugin=${product}"/>
			<arg value="-Dreport=${product}"/>
			<arg value="-Declipse-home=${baseLocation}"/>
			<arg value="-DlibraryFile=${baseLocation}/plugins/org.eclipse.test_${fullEclipseVersion}/library.xml"/>
		</java>
		<move file="${baseLocation}/${product}.xml" tofile="${artifacts}/gmf-tests/${product}.xml"/>
		<move file="${baseLocation}/${product}.AllTests.xml" tofile="${artifacts}/gmf-tests/${product}.AllTests.xml"/>		
		<antcall target="uninstallTests"/>
	</target>
	
	<target name="installTests" unless="testsExist">		
		<antcall target="installTestsFromMirror"/>
		<antcall target="installTestsFromZip"/>
	</target>
	
	<target name="installTestsFromMirror" if="usingMirror">
		<echo message="Installing GMF tests for execution..."/>
		<property file="${buildDirectory}/maps/org.eclipse.gmf.releng/maps/build.cfg"/>
		<ant antfile="${scripts}/update.xml" target="install">
			<property name="featureId" value="${product}" />
			<property name="version" value="${gmfVersion}" />
		</ant>
		<!-- FIXME! It seems the update manager does not recognize/respect the unpack="true" option in a feature.xml file. -->
		<unzip dest="${baseLocation}/plugins/${product}_${gmfVersion}" overwrite="true" src="${baseLocation}/plugins/${product}_${gmfVersion}.jar" />
		<delete file="${baseLocation}/plugins/${product}_${gmfVersion}.jar"/>
	</target>
	
	<target name="installTestsFromZip" unless="usingMirror">
		<echo message="Installing GMF tests from zip file..."/>
		<unzip dest="${baseLocationRoot}/" overwrite="true" src="${artifacts}/gmf-tests/${product}-${buildId}.zip" />
	</target>
	
	<!-- TODO: easy to uninstall if installed via update manager... need to add removal if installed from zips
			   (for now, just use cleanBase option to uninstall ;) -->
	<target name="uninstallTests">
		<echo message="Uninstalling tests from base ..."/>
		<ant antfile="${scripts}/update.xml" target="uninstall">
			<property name="featureId" value="${product}" />
			<property name="version" value="${gmfVersion}" />
		</ant>
	</target>

	<!-- ===================================================================== -->
	<!-- Steps to do to publish the build results -->
	<!-- ===================================================================== -->
	<target name="publish">
	</target>

	<!-- ===================================================================== -->
	<!-- Default target                                                        -->
	<!-- ===================================================================== -->
	<target name="noDefault">
		<echo message="You must specify a target when invoking this file" />
	</target>

</project>
